# Do not modify
NUM_VIDEOS = 48
IMAGES_PER_VIDEO = 100
NUM_IMAGES = NUM_VIDEOS * IMAGES_PER_VIDEO
ORIGINAL_IMG_SIZE = (1280, 720)

# Modifiable
BINARIZE_THRESHOLD = 0.35 # This must be equal to the value used to binarize in process_images.m

IMG_SIZE = (640, 368) # 368 is to avoid rounding the resolution in the encoder
DATASET_SPLIT_SEED = 6356 #1337 # To shuffle videos 

VALIDATION_DATA_SPLIT = 1 / 6
TEST_DATA_SPLIT = 1 / 6
TRAINING_DATA_SPLIT = 1 - VALIDATION_DATA_SPLIT - TEST_DATA_SPLIT

assert(VALIDATION_DATA_SPLIT + TEST_DATA_SPLIT + TRAINING_DATA_SPLIT == 1)

TRAINING_DATASET_VIDEO_COUNT = round(NUM_VIDEOS * TRAINING_DATA_SPLIT)
TEST_DATASET_VIDEO_COUNT = round(NUM_VIDEOS * TEST_DATA_SPLIT)
VALIDATION_DATASET_VIDEO_COUNT = round(NUM_VIDEOS * VALIDATION_DATA_SPLIT)

assert(TRAINING_DATASET_VIDEO_COUNT + TEST_DATASET_VIDEO_COUNT + VALIDATION_DATASET_VIDEO_COUNT == NUM_VIDEOS)